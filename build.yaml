targets:
  $default:
    builders:
      drift_dev:
        options:
          store_date_time_values_as_text: true
          named_parameters: true
      json_serializable:
        options:
          field_rename: snake
          explicit_to_json: true
