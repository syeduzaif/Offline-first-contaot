/// Re-exported constant for tests so they don't depend on the full
/// `core/constants/app_constants.dart` (which may pull in Flutter bindings
/// transitively as services grow).
const String kindUsers = 'users';
